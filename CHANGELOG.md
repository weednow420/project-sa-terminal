# PROJECT S-A TERMINAL // ЖУРНАЛ ИЗМЕНЕНИЙ (CHANGELOG)

Все заметные изменения проекта, обновления интерфейса и версионирование фиксируются в этом файле.

## [v1.3.1-ingest] — 2026-09-09

#### 1. Интеграция Мастер-Инструкции Мульти-Агентного Конвейера
- **Канонический документ [`cards/MASTER_PROCESSING_INSTRUCTION.md`](cards/MASTER_PROCESSING_INSTRUCTION.md)**:
  - Формализован Мастер-Промпт v1.2 для автономной пакетной обработки текстовых массивов и видео-транскриптов любой длительности (от 15 минут до 2+ часов).
  - Специфицированы роли и параметры 5 агентов:
    1. `Агент-Диспетчер` (шлюз-роутер, отсечение NULL-шума).
    2. `Агент-Архивариус` (фактология, ретро-технологии, физические законы).
    3. `Агент-Метафизик` (очищенная от пафоса метафизика, тестируемые гипотезы).
    4. `Агент-Когнитивист` (паттерны внимания, инфо-пост, изоляция от социума).
    5. `Агент-Аудитор` (сетевой фактчекинг, экспертный комментарий `auditor_comment`, шкала `evidence_level`).
  - **Карантин поп-науки**: ненаучные спекуляции и мифы изолируются в домен `ИНЦИДЕНТ b181` с меткой `#миф` и статусом `НЕ ДОКАЗАНО` / `МАЛО ДОКАЗАТЕЛЬСТВ`.
  - **Закрытая таксономия Zettelkasten-тегов**: жесткий словарь из 5 блоков (`МЕТА`, `СОМАТИКА`, `КОГНИТИВИСТИКА`, `ИЗОЛЯЦИЯ`, `ТЕХНО`).
  - **Стандартизированный JSON-вывод**: строгий формат карточки для прямого импорта в базу данных Fastify/SQLite.
- **Актуализация паспорта проекта**: раздел 3.9 зафиксировал архитектуру конвейера в [ПАСПОРТ_ПРОЕКТА_PROJECT_SA.md](ПАСПОРТ_ПРОЕКТА_PROJECT_SA.md).

---

## [v1.3-alpha] — 2026-09-09

#### 1. Кибернетическое Главное Меню и Био-Монитор (по утвержденному макету)
- **Строка заголовка Telegram Top Bar (`.telegram-top-bar`)**:
  - Моноширинный статус: `Terminal S-A // Базис`, системная плашка `SYS_ACTIVE // KB-181`, кнопка `[× ЗАКРЫТЬ]`.
- **Секция Био-Монитора (`#bio-monitor-section`)**:
  - Карточка микрочипа кота CAT181b с контактной шиной, CRT-индикацией и статусом `СИНХРОНИЗАЦИЯ: 100%`.
  - Циклический переключатель 6 кинематических состояний (`ПОКОЙ`, `МУРЧАНИЕ`, `ШАЛОСТЬ`, `СОН`, `ТРЕВОГА`, `ГЛИТЧ`) прямо по тапу на чип.
  - Панель из 3 терминальных кнопок быстрого доступа:
    1. `[ ТЕМА: КОНСОЛЬ / E-INK ]` (с сохранением в `localStorage`).
    2. `[ ЗВУК: ВКЛ / ВЫКЛ ]` (тумблер процедурного Web Audio API).
    3. `[ СБРОС: КАТ181b (ПОКОЙ) ]` (мгновенный возврат кошки из любого состояния в режим покоя).

#### 2. Кибернетический Плавающий Dock (`#terminal-dock-container`)
- **Аппаратный фрейм с угловыми скобками `┌ ┐ └ ┘`**:
  - Фиксированный плавающий док в нижней части экрана с кинематографичным оформлением.
  - 4 системных таба: `[01] БАЗИС`, `[02] ГРИМУАР`, `[03] КОНТУР`, `[04] ЛАБ-181`.
  - Инверсная индикация активной вкладки (`bg-[#e6e6e6] text-[#0a0a0a]`).
  - Статусная строка подвала: `NODE: S-A-LOCAL`, `ENC: GOST-28147`, `STABILITY: 99.8%`, `BOT: @MyGeminiRuHelper_bot`.

#### 3. Чистая Триада Доменов Гримуара
- **Реструктуризация каталога**:
  - Полный отказ от 4-й категории `0_basis` как контентной категории гримуара.
  - Строгая 3-доменная архитектура: `01 СОМАТИКА`, `02 КОГНИТИВИСТИКА`, `03 ИЗОЛЯЦИЯ`.
  - 12 сфокусированных подкатегорий (по 4 на каждый домен).
  - 24 атомарные карточки в жестком формате 5 блоков: `ТЕЗИС`, `ФИЗИОЛОГИЯ`, `ПРОТОКОЛ`, `КРИТЕРИЙ`, `СЕТЬ СВЯЗЕЙ S-A` с Zettelkasten-синапсами.
  - 12 интерактивных плашек в главном хабе со скошенными углами и notch-акцентами (`01`, `02`, `03`) для прямого перехода в нужный раздел.

#### 4. Облачный Хостинг 24/7 (Render.com + Docker)
- **Продакшн Docker-контейнер**:
  - Сборка на `node:20-bookworm-slim` + Python 3.11 (`aiogram`, `python-dotenv`).
  - Скрипт `entrypoint.sh` запускает бэкенд Fastify на `PORT=10000` и фонового бота Aiogram.
- **Интеграция с Telegram Bot API**:
  - Настройка `setChatMenuButton` на постоянный HTTPS URL `https://project-sa-terminal.onrender.com`.
  - Бесшовное открытие WebApp внутри Telegram круглосуточно.
- **CI/CD Deploy Hook**:
  - Моментальный автодеплой при вызове секретного вебхука.
  - Полный вывод из эксплуатации локального `launcher.js` и процесса ngrok.

---

## [v1.2-alpha] — 2026-09-09

#### 1. Расширенная Матрица 6 Состояний Кибер-Симбионта CAT181b
- **Интеграция авторского пакета анимаций `cats_ready`**:
  - Интегрированы 6 высококачественных 100-кадровых APNG-анимаций (128×128, плавный 10-секундный цикл) с фирменной стилистикой (черный графит, изумрудно-мятные глаза, красный ошейник):
    1. `cat_idle.png` (`[IDLE / ПОКОЙ]`): спокойное дыхание и наблюдение.
    2. `cat_purr.png` (`[PURR / МУРЧАНИЕ]`): покачивание, закрытые глаза `^ ^`, ноты и сердечки.
    3. `cat_mischief.png` (`[MISCHIEF / ШАЛОСТЬ]`): ухмылка, вздернутая бровь, виляние хвостом.
    4. `cat_sleep.png` (`[SLEEP / СОН]`): гибернация, кот свернулся калачиком, `z z Z`.
    5. `cat_alert.png` (`[ALERT / ТРЕВОГА]`): испуг, дрожь, вздыбленная шерсть.
    6. `cat_glitch.png` (`[GLITCH / ГЛИТЧ]`): киберпанк-помехи, скан-линии и табличка сбоя.
- **Интерактивный контур и Тамагочи-цикл**:
  - Обновлен модуль `frontend/cat_symbiont.js`:
    - Кнопка «Покормить» → процедурный хруст `playCatCrunch` + переход в режим нежного мурлыканья `purr`.
    - Кнопка «Поиграть» → гармонический тритон `playCatPlay` + переход в режим озорной шалости `mischief`.
    - Кнопка «Погладить» → вибро-модулированное мурчание ~68 Гц `playCatPurr` + Telegram Haptic + переход в `purr`.
    - Кнопка «Санитария» → продувка сенсоров, сброс глитча/тревоги, возврат в `idle`.
  - **Авто-гибернация**: при отсутствии активности оператора > 50 секунд кошка плавно переходит в глубокий сон (`sleep`), а при первом клике или тапе в шапке просыпается в `idle`.
- **Интерфейс Лаборатории**:
  - Матрица из 6 кнопок режимов (`cat-matrix-grid`) в строгом кибер-стиле.
  - Адаптивный размер отсека чипа: 128×128 пиксель-в-пиксель, безопасный нижний отступ для мобильного просмотра.

---

## [v1.1-alpha] — 2026-09-09

#### 1. Кибер-Симбионт CAT181b и Полное Исключение Аксолотля
- **Стилизация анимаций Steam Community**:
  - Интегрированы 3 высокодетализированные APNG анимации (`cat_sit.png`, `cat_peek.png`, `cat_action.png`).
  - Проведена глубокая пиксельная стилизация:
    - Конвертация янтаря глаз в чистый изумрудно-мятный цвет (`#8fe09b` / `#7fd68d`).
    - Нейтрализация теплых коричневых пятен в глубокий угольно-черный мех.
    - Добавление сигнального красного ошейника (`#d7232d`) с золотым жетоном (`#e2b342`).
- **Интерактивный симбионт в шапке (`#cat-header-container`)**:
  - Кошка CAT181b сидит под строкой меню терминала.
  - Тап вызывает кошачий синтезированный звук `playCatMeow()`, тактильный отклик и временную смену анимации (3.5 сек).
- **Экран Лаборатории (`#view-terrarium-lab`)**:
  - Карточка микрочипа `[CHIP: CAT181b] S-A TERMINAL` с контактными выводами шины.
  - 3 переключателя анимаций: `[ 01. НАБЛЮДЕНИЕ ]`, `[ 02. ВЫГЛЯДЫВАНИЕ ]`, `[ 03. АКТИВНОСТЬ ]`.
  - 4 кнопки ухода: `[ 🐟 ПОКОРМИТЬ ]`, `[ 🧶 ПОИГРАТЬ ]`, `[ 🖤 ПОГЛАДИТЬ ]`, `[ 🧹 САНИТАРИЯ ]` с процедурным звуковым сопровождением (хруст, колокольчик, вибро-мурчание).
- **Полное удаление аксолотля**:
  - Удален холст `axolotl-canvas-face`, скрипт `terrarium.js`, ассет `cyber_axolotl.png`, вкладка выбора и все связанные вызовы.

---

## [v0.4a] — 2026-09-08

#### 1. Процедурный Ретро-Звуковой Движок (Web Audio API / SoundFX)
- **Автономный микро-синтезатор без файлов (`frontend/audio.js`)**:
  - Полная генерация звуковых волн процедурно на лету с помощью Web Audio API (осцилляторы, фильтры и огибающие громкости).
  - Нулевая зависимость от внешних `.mp3` или `.wav` файлов — мгновенный отклик без задержек и ошибок кэширования.
- **Аутентичная палитра звуков советской кибернетики**:
  - **Герконовый щелчок клавиш (`playKeyClick`)**: сухой щелчок релейных контактов клавиатуры 3×4 с динамическим повышением тона при заполнении ячеек.
  - **Механический отбой (`playKeyBackspace`)**: глухой щелчок при стирании символов и сбросе буфера.
  - **Гармонический гонг допуска (`playAccessGranted`)**: трезвучие ГОСТ-КБ-181 (E5-A5-E6) при успешной верификации ключа.
  - **Тревожный зуммер сбоя (`playAccessDenied`)**: диссонирующий скрежет ошибки доступа с биениями частот 148/155 Гц.
  - **Рубильник тем оформления (`playThemeSwitch`)**: плотный механический щелчок при переключении между Консолью и E-Ink.
  - **Аварийный зуммер (`playEmergencyAbort`)**: нисходящий предупреждающий вой сирены при экстренном сбросе.
  - **Водяной отклик террариума (`playBioPulse`)**: гидроакустический резонансный клик при тапе по аксолотлю b181 в лаборатории.
- **Тумблер звука**:
  - Кнопка `[ 🔊 ЗВУК: ВКЛ / 🔇 ВЫКЛ ]` добавлена в шапку терминала и в телеметрию шлюза.
  - Сохранение пользовательской настройки в `localStorage` (`sa_sound_enabled`).
- **Синхронизация с мобильными платформами (Telegram Mini App)**:
  - Ленивая разблокировка аудиоконтекста по первому жесту пользователя (Touch / Click) без предупреждений браузера об автовоспроизведении.

#### 2. Интеграция с дизайн-системой Google Stitch (MCP)
- Настроен защищенный мост MCP в `~/.gemini/config/mcp_config.json` с официальным пакетом `@_davideast/stitch-mcp`.
- Синхронизация с проектом `658864492986012622` («Project Overview») для прямого импорта дизайн-экранов и компонентов в бот.

---

## [v0.3a] — 2026-09-08

#### 1. Терминал Защиты // ГОСТ-КБ-181-СПЕЦ (Шлюз Авторизации)
- **Аутентичный бруталистский экран входа**:
  - Базовый минималистичный пин-пад заменен на полнофункциональную терминальную стойку безопасности в духе советской кибернетики закрытых НИИ.
- **Индустриальная шапка телеметрии**:
  - Системный статус `SYS_ONLINE // KB-181`, идентификатор узла `NODE_ID: #09-SU`, протокол `ГОСТ-КБ-181-СПЕЦ`.
  - Статусная плашка `[ СОСТОЯНИЕ: ТРЕБУЕТСЯ КАЛИБРОВКА ]` с пульсирующим тревожным маркером `● ТРЕВОГА-0`.
  - Монитор первичных датчиков: `БИО-ДАТЧИК: ОЖИДАНИЕ`, `ДЖИТТЕР: 0.042 ms`, `ЭНТРОПИЯ: 18.4% [НОРМА]`, `РЕВИЗИЯ: v0.3a//С-09`.
- **Моноширинный визуализатор слотов ключа**:
  - 4 крупных терминальных ячейки (54×62 px) с состояниями курсора `_`, заполнения `■` и резерва `·`.
  - Зеленая строб-вспышка при успехе (`ДОСТУП РАЗРЕШЕН`) и контрастная красная подсветка с вибрацией и встряской при неверном векторе.
- **Полноразмерная жесткая клавиатура 3×4**:
  - Кнопки `[ 1 ]`...`[ 0 ]`, клавиша быстрой очистки `[ СБРОС ]`, клавиша посимвольного стирания `[ ⌫ ]`.
  - Полная поддержка аппаратной клавиатуры ПК (цифры 0-9, Backspace, Escape/Delete).
  - Тактильная виброотдача (**Telegram Haptic Feedback**).
- **Живой системный лог телеметрии (`#sys-log`)**:
  - Моноширинный транслятор протокола с временными метками `> HH:MM:SS`:
    - Инициализация шины данных и ожидание ключа;
    - Фиксация очистки буфера или удаления символов;
    - Логирование инцидента b181 и отклонения неверных векторов;
    - Подтверждение распечатывания резервуара.
- **Индикатор наполнения буфера**:
  - Динамическая псевдографическая шкала `[ ■ ■ □ □ ] 50%`.
- **Кнопка аварийного прерывания**:
  - `[ ! ЭКСТРЕННЫЙ СБРОС СЕССИИ ! ]` со строгим сбросом регистров и тактильным сигналом ошибки.
- **Кинематографичный CRT-сканлайн**:
  - Фоновая микросетка телевизионных строк кинескопа поверх экрана без падения производительности.
- **Идеальная поддержка Тёмной и Светлой темы**:
  - Стилизовано на чистых CSS Custom Properties без внешнего Tailwind CDN с мгновенной отрисовкой на E-Ink и OLED.

---

## [v0.2a] — 2026-09-08

#### 1. Био-террариум Аксолотля (Кибер-Симбионт b181 + Медленный кликер)
- **Визуальный образ: Кибернетический Симбионт b181**:
  - Питомец переведен из базового мультяшного стиля в аутентичный **кибер-аксолотль в духе Cassette Futurism**: светлые костяные экзо-пластины, рунический антенный глиф на лбу, механические сочленения, бирюзовые световоды и зеленые точечные LED-матричные глаза CRT.
- **Анимированная кибер-мордочка в шапке (`#bio-terrarium`)**:
  - Компактная некликабельная миниатюра (`pointer-events: none`) прямо под системным разделителем.
  - Оснащена живой пиксельной анимацией: покачивание 3 пар механических жабр с неоновыми кончиками, дыхание, рунический глиф, мерцающие матричные зеленые светодиоды глаз (моргание, прищур `^ ^` при сытости, янтарный индикатор при истощении).
  - Скрыта до успешной авторизации оператора.
- **Новая кнопка «ЛАБОРАТОРИЯ» в нижнем доке**:
  - В нижнюю панель навигации рядом с кнопкой `БАЗИС` добавлена кнопка `[ ЛАБОРАТОРИЯ / ΕΡΓΑΣТΗΡΙОН ]`.
- **Полноценный экран тамагочи-лаборатории (`#view-terrarium-lab`)**:
  - **Живой многосегментный Кибер-Аксолотль (True Pixel-Art)**: процедурная 16/32-bit пиксельная модель без сглаживания (`image-rendering: pixelated; imageSmoothingEnabled = false`).
  - **Плавная серпентиновая гидродинамика плавания**: 6-сегментный изгибающийся волновой хвост с фазовым запаздыванием, мягкий крен корпуса по курсу.
  - **4 шарнирные гребущие лапки**: циклическое гребное движение с раздельными пальцами-манипуляторами.
  - **6 механических ветвей жабр**: независимое колыхание под сопротивлением воды и реактивное распушение при тапе.
  - **Бегущие пакеты данных**: неоновые световые импульсы (`#38e8d8` / `#a3fff6`), скользящие вдоль позвоночника в реальном времени.
  - **Зеленые CRT-матричные LED-глаза**: цифровые индикаторы с морганием и сменой выражения при сытости / истощении.
  - **Интерактивный отклик на тап**: реактивный импульс ускорения (`speedMultiplier`), электрические искры разряда (`labSparks`), гидроакустические сонарные волны (`labSonarWaves`) и цепочка микропузырьков из жабр.
  - Шкалы состояния: `БИО-ТОНУС (СЫТОСТЬ)` и `ЧИСТОТА СРЕДЫ (ЭНТРОПИЯ)`.
  - **Системные протоколы взаимодействия**:
    - `[ ИНФУЗИЯ: ТАЛАЯ ВОДА ]` — восстанавливает Био-тонус (+30%).
    - `[ ФИЛЬТРАЦИЯ КОНТУРА ]` — сбрасывает накопленную энтропию среды.
  - **Медленная прокачка контура**:
    - `УСИЛИТЕЛЬ ИМПУЛЬСА` (увеличение силы клика +1, сбалансированный рост цены x1.8).
    - `ГЕНЕРАТОР ПУЗЫРЬКОВ` (пассивный приток импульсов в секунду, рост цены x1.7).
- **Оффлайн-расчет и сохранение**:
  - Автоматический расчет оффлайн-добычи импульсов при отсутствии оператора (до 8 часов).
  - Полноценное локальное сохранение в `localStorage` (`SA_TERRARIUM_STATE_V1`).

---

## [v0.1a] — 2026-09-08

### 1. Навигация и главное меню (Единая точка входа)
- **Кнопка «БАЗИС» в нижнем доке**: трансформирована в единую кнопку вызова Главного Меню системы.
- **Главное Меню управления**:
  - Создана компактная модальная панель в 1-bit терминальном стиле без лишней прокрутки.
  - Структурированы основные разделы:
    - `[01] СОМАТИКА` (прямой переход к карточкам телесных практик)
    - `[02] КОГНИТИВИСТИКА` (прямой переход к когнитивным алгоритмам)
    - `[03] ИЗОЛЯЦИЯ` (прямой переход к протоколам депривации)
    - `[⚡] ИНСТРУМЕНТЫ` (интерактивный хаб утилит)
    - `[★] ЗАКЛАДКИ` (сохраненные оператором карточки)
    - `[◎] НАБЛЮДЕНИЯ` (модуль личных заметок «Источник» и отправки серверных сообщений «Послание»)
    - `[i] БАЗИС (ИНФО)` (карточки базового лора и архитектуры системы)
- **Очистка экранов**:
  - Убраны дублирующие верхние вкладки разделов и инструментов.
  - Удалены загромождавшие экран цепочки breadcrumbs (`← БАЗИС / ИНСТРУМЕНТЫ / ИНСТРУМЕНТ`, `← БАЗИС / ЗАКЛАДКИ`).
  - Экран детального просмотра карточки получил аккуратную панель действий: `[ ← НАЗАД К СПИСКУ ]` и `[ 🔖 В ЗАКЛАДКИ ]`.
  - Стартовый экран очищен от автоматической загрузки карточек и встречает чистым приветствием с подсказкой открыть меню.

### 2. Новый функционал (Интерактивные инструменты)
- **05 // КАЛЬКУЛЯТОР ФАЗ СНА**:
  - Интерактивный расчет 90-минутных циркадных циклов быстрого и медленного сна.
  - Режим «Если уснуть прямо сейчас» с учетом 15 минут на засыпание и градацией циклов (Оптимум / Норма / Критично).
  - Режим обратного расчета времени отбоя по заданному времени будильника.
- **Навигация в утилитах**:
  - Добавлена быстрая кнопка `[ ← ВСЕ ИНСТРУМЕНТЫ ]` внутри каждого открытого инструмента для мгновенного возврата к списку.

### 3. Telegram-бот и развертывание
- **Подключение нового бота**: `@MyGeminiRuHelper_bot` с автоматическим распознаванием и фолбэком токена.
- **Оформление бота через Telegram Bot API**:
  - Установлено системное имя `Terminal S-A`.
  - Задано превью-описание (about) и краткое описание в профиле в стилистике лора Project S-A.
  - Настроены быстрые команды `/start`, `/profile`, `/status`.
  - Закреплена постоянная кнопка чата Menu Button `>> ТЕРМИНАЛ S-A` для запуска Mini App.
  - В чате активирована постоянная нижняя клавиатура быстрого доступа (`>> ОТКРЫТЬ ТЕРМИНАЛ`, `👤 ПРОФИЛЬ ОПЕРАТОРА`, `📡 СТАТУС СИСТЕМЫ`).
- **Автоматический лаунчер (`launcher.js`)**:
  - Привязка закрепленного HTTPS-домена `require-cursive-gone.ngrok-free.dev`.
  - Автоматическое обновление кнопки меню Telegram при каждом старте.

### 4. Версионирование
- Метка текущей версии `v0.1a` зафиксирована внизу страницы терминала (в футере).
- Подключен cache-busting для статических ассетов (`app.js?v=0.1a`, `style.css?v=0.1a`).
- Создан данный файл `CHANGELOG.md` для ведения истории всех релизов.

---

## [v0.1.0-MVP] — 2026-09-07
- Начальный релиз Telegram Mini App.
- Базовые разделы: Соматика, Когнитивистика, Изоляция, Базис.
- Утилиты: Талая вода, Расклад Таро, Дыхание (4-4-4), Журнал инцидентов b181.
- Модуль заметок и синхронизация с базой данных SQLite.
