# 🗂️ ПОЛНЫЙ ПАСПОРТ ПРОЕКТА: PROJECT S-A TERMINAL

---

## 1. КОНЦЕПЦИЯ, ФИЛОСОФИЯ И ЛОР

**PROJECT S-A TERMINAL (Проект С-А)** — это автономное специализированное веб-приложение (**Telegram Mini App / TMA**), созданное на стыке **советской кибернетики закрытых НИИ**, **аналогового ретрофутуризма (Cassette Futurism)**, **биохакинга** и **радикального анти-дофамина**.

### 1.1. Философия системы
1. **Анти-дофаминовый подход:** Интерфейс не развлекает, не устраивает «праздник конфетти» и не провоцирует компульсивный скролл. Он дисциплинирует, собирает телеметрию и калибрует состояние оператора.
2. **Терминология контура:**
   - **Оператор** — человек, синхронизированный с терминалом через 4-значный ключ доступа.
   - **Био-датчик** — физическое тело и нервная система Оператора.
   - **Контур** — замкнутая цепь взаимного наблюдения и обратной связи.
   - **Гримуар** — систематизированная база знаний чистой Триады: Соматика, Когнитивистика, Изоляция.
   - **Инцидент b181** — универсальный код аномалий, сбоев или закрытых протоколов очистки.
   - **Талая вода** — процесс фракционного вымораживания дейтерия для биологической перезагрузки клеточной матрицы.
3. **Эстетика 1-Bit / E-Ink:**
   - Строгий брутализм, отсутствие скруглений (`border-radius: 0px`).
   - Моноширинные шрифты (`JetBrains Mono`, `Space Mono`, `Fira Code`).
   - Прямоугольные фреймы, аппаратные уголки (`┌ ┐ └ ┘`), ASCII-символика (`[ ]`, `■`, `═══`, `───`).
   - Две контрастные темы: **Тёмная консоль** (`#0a0a0a`) и **Светлая электронная бумага (E-Ink)** (`#f5f5f0`).

---

## 2. АРХИТЕКТУРА И СТЕК ТЕХНОЛОГИЙ

```
project-sa-terminal/
├── backend/            # Fastify REST API + SQLite (Node.js)
│   ├── src/
│   │   ├── db/         # Схема TERMINAL_DB, сиды категорий и карточек
│   │   └── routes/     # categories, cards, auth, operators, messages
│   └── index.js        # Точка входа сервера (порт 3000 локально / 10000 в облаке)
├── bot/                # Telegram-бот (Python 3 + Aiogram 3)
│   └── main.py         # Обработка /start, Telegram Bot Menu Button
├── frontend/           # Клиентская часть (Vanilla JS, Web Audio API, CSS)
│   ├── index.html      # Терминальное SPA: Top Bar, Био-Монитор, Триада, Dock
│   ├── style.css       # Терминальные стили: CRT-эффекты, E-Ink, адаптивный лейаут
│   ├── app.js          # Ядро интерфейса, навигация, док, био-монитор, заметки
│   ├── cat_symbiont.js # Машина состояний CAT181b (6 режимов) + уход
│   ├── audio.js        # Процедурный синтезатор Web Audio API (0 КБ внешних файлов)
│   └── assets/         # 6 APNG-анимаций кибер-кошки (128×128, 100 кадров)
├── cards/              # База знаний Гримуара (Чистая Триада // 12 категорий // 24 карточки)
│   ├── 1_somatics/     # СОМАТИКА (ЦНС, Дыхание, Тонус, Вода)
│   ├── 2_cognitivism/  # КОГНИТИВИСТИКА (Внимание, Циркады, Фокус, Пост)
│   └── 3_isolation/    # ИЗОЛЯЦИЯ (Депривация, Карантин, Холод, Молчание)
├── Dockerfile          # Двухъязычный контейнер (Node.js 20 + Python 3)
├── entrypoint.sh       # Скрипт параллельного запуска Fastify и Aiogram-бота
├── render.yaml         # Конфигурация инфраструктуры Render.com
└── launcher.js         # Резервный локальный лаунчер (Node.js + Python)
```

- **Backend:** Node.js, Fastify, SQLite (`better-sqlite3` / `sqlite3`). Быстрый отклик (<15 мс), валидация `initDataUnsafe` из Telegram, сессии по PIN-коду.
- **Frontend:** Чистый Vanilla JS без тяжелых фреймворков. Web Audio API для процедурного звука, чистая семантическая верстка. Сохранение в `localStorage`.
- **Bot:** Python 3.11 + Aiogram 3. Интеграция с Telegram Bot API, настройка кнопки меню чата (`setChatMenuButton`) на постоянный HTTPS URL.
- **Облачная инфраструктура:** **Render.com Cloud (Docker, 24/7)**, постоянный домен `https://project-sa-terminal.onrender.com`, Deploy Hook для моментального автоматического обновления.

---

## 3. ЧТО УЖЕ ПОЛНОСТЬЮ РЕАЛИЗОВАНО (v1.3-alpha // CLOUD-DOCK-CYBER)

### 🔒 3.1. Терминал Защиты // ГОСТ-КБ-181-СПЕЦ (Шлюз Авторизации)
- **Индустриальная стойка безопасности**:
  - Полный доступ к терминалу заблокирован до ввода пин-кода (по умолчанию `1810`).
  - Все функциональные экраны скрыты до подтверждения авторизации.
  - Шапка телеметрии (`SYS_ONLINE // KB-181`, `NODE_ID: #09-SU`, плашка `● ТРЕВОГА-0`, счетчики джиттера и энтропии).
- **Моноширинный визуализатор слотов**:
  - 4 терминальные ячейки с состояниями `_`, `■`, `·`.
  - Зеленый строб-индикатор допуска, красная подсветка с вибрацией и встряской при неверном векторе.
- **Полноразмерная клавиатура 3×4**:
  - Кнопки `[ 1 ]`...`[ 0 ]`, клавиша очистки `[ СБРОС ]`, клавиша стирания `[ ⌫ ]`.
  - Поддержка физической клавиатуры ПК и Telegram Haptic Feedback.
- **Живой системный лог телеметрии (`#sys-log`)**:
  - Моноширинный лог с таймштампами `> HH:MM:SS`, шкала буфера `[ ■ ■ □ □ ] 50%`.
  - Кнопка `[ ! ЭКСТРЕННЫЙ СБРОС СЕССИИ ! ]`.

### 🧭 3.2. Кибернетическое Меню и Секция Био-Монитора (по макету)
- **Telegram Top Bar (`.telegram-top-bar`)**:
  - Моноширинная строка: `Terminal S-A // Базис`, системный статус `SYS_ACTIVE // KB-181`, кнопка `[× ЗАКРЫТЬ]`.
- **Био-Монитор (`#bio-monitor-section`)**:
  - **Левая колонка**: Карточка чипа `CAT181b` с контактными выводами, CRT-статусом синхронизации (`100%`) и бейджем текущего режима (`ПОКОЙ`, `МУРЧАНИЕ`, `ШАЛОСТЬ`, `СОН`, `ТРЕВОГА`, `ГЛИТЧ`). Клик переключает состояния по кругу.
  - **Правая колонка**: Заголовок `// БИО-МОНИТОР`, описание контура и панель из **3 терминальных кнопок быстрых настроек**:
    1. `[ ТЕМА: КОНСОЛЬ / E-INK ]` — переключение цветовой схемы с сохранением в `localStorage`.
    2. `[ ЗВУК: ВКЛ / ВЫКЛ ]` — переключение звукового движка Web Audio API.
    3. `[ СБРОС: КАТ181b (ПОКОЙ) ]` — возврат кошки из тревоги, сна или глитча в базовый покой.

### 📚 3.3. Чистая Триада Доменов Гримуара
Гримуар освобожден от вспомогательных категорий (`0_basis` перенесен в системную справку) и структурирован в строгую Триаду:
1. **[01] СОМАТИКА** (акцентный цвет — изумруд `#10b981`):
   - `ЦНС & Блуждающий нерв`
   - `Дыхание & Газообмен`
   - `Мышечный тонус`
   - `Гидродинамика & Вода`
2. **[02] КОГНИТИВИСТИКА** (акцентный цвет — неоновый циан `#06b6d4`):
   - `Паттерны внимания`
   - `Световой спектр & Циркады`
   - `Сенсорный фокус`
   - `Информационный пост`
3. **[03] ИЗОЛЯЦИЯ** (акцентный цвет — пурпур `#a855f7`):
   - `Сенсорная депривация`
   - `Цифровой карантин`
   - `Холодовой протокол`
   - `Молчание & Пустота`

- **Архитектура карточек (24 атомарные карточки)**:
  Каждая карточка содержит ровно 5 обязательных блоков:
  1. `ТЕЗИС` — концептуальное определение.
  2. `МЕХАНИЗМ (ФИЗИОЛОГИЯ)` — биологическая и неврологическая основа.
  3. `ПРОТОКОЛ ДЕЙСТВИЯ` — точные пошаговые инструкции оператора.
  4. `КРИТЕРИЙ ВЫХОДА` — измеримый результат завершения практики.
  5. `СЕТЬ СВЯЗЕЙ S-A` — Zettelkasten-синапсы со ссылками на смежные карточки других доменов.
- **Интерактивные плашки в хабе**: Клик на любую из 12 плашек в главном меню мгновенно открывает Гримуар с фильтром по выбранному подразделу.

### ⚓ 3.4. Кибернетический плавающий Dock (`#terminal-dock-container`)
- **Аппаратная рамка**: Аутентичные угловые скобки `┌ ┐ └ ┘` с тонкими контурными линиями.
- **4 системных таба навигации**:
  - `[01] БАЗИС` — возврат в главный терминальный хаб и био-монитор.
  - `[02] ГРИМУАР` — база знаний Триады с фильтрами и поиском.
  - `[03] КОНТУР` — практические утилиты и заметки оператора.
  - `[04] ЛАБ-181` — отдельный глубокий отсек ухода за кибер-симбионтом.
- **Инверсная индикация**: Активный таб инвертируется (светлый фон, темный текст).
- **Подвальная телеметрия**: Нижняя статусная строка с параметрами шины (`NODE: S-A-LOCAL`, `ENC: GOST-28147`, `STABILITY: 99.8%`, `BOT: @MyGeminiRuHelper_bot`).

### 🐱 3.5. Кибер-Симбионт CAT181b
- **Матрица 6 состояний**:
  - Полный пакет из 6 авторских 100-кадровых APNG-анимаций (128×128):
    1. `IDLE` (Покой) — ровное дыхание, бдительный фокус.
    2. `PURR` (Мурчание) — гармонизация, закрытые глаза `^ ^`, ноты и сердечки.
    3. `MISCHIEF` (Шалость) — ухмылка, вздернутая бровь, виляние хвостом.
    4. `SLEEP` (Сон) — гибернация, кот свернулся калачиком, `z z Z`.
    5. `ALERT` (Тревога) — испуг, дрожь сервоприводов, вздыбленная шерсть.
    6. `GLITCH` (Глитч) — киберпанк-помехи, скан-линии, `ERROR: BIT CORRUPT/COM`.
- **Авто-гибернация**: при бездействии оператора > 50 секунд кошка засыпает, клик по экрану мгновенно будит её.
- **Тамагочи-контур в Лаборатории**:
  - Кнопки ухода: `[ 🐟 ПОКОРМИТЬ ]`, `[ 🧶 ПОИГРАТЬ ]`, `[ 🖤 ПОГЛАДИТЬ ]`, `[ 🧹 САНИТАРИЯ ]`.
- **Процедурный аудио-движок (`frontend/audio.js`)**:
  - 100% синтез через Web Audio API (хруст корма, колокольчик игры, вибрация мурчания 68 Гц, клики герконов, зуммеры допуска/сбоя). 0 КБ внешних аудиофайлов.

### ⚡ 3.6. Интерактивный хаб инструментов Контура
1. **§01 // Калькулятор талой воды**: расчет фракционного вымораживания дейтерия.
2. **02 // Утренний расклад Таро**: 22 Старших Аркана как психотехнологические архетипы фокуса.
3. **03 // Био-ритм дыхания (4-4-4-4)**: квадратный таймер вегетативной стабилизации.
4. **04 // Журнал инцидентов b181**: регистратор джиттера и рассинхронизации.
5. **05 // Калькулятор фаз сна**: расчет 90-минутных циркадных циклов.

### 📝 3.7. Наблюдения (Источник & Послание)
- **ИСТОЧНИК (Локально)**: автономная записная книжка оператора в `localStorage`.
- **ПОСЛАНИЕ (Сервер)**: отправка зашифрованных рапортов на Fastify-бэкенд с сохранением в SQLite.

### ☁️ 3.8. Автономная облачная инфраструктура 24/7 (Render.com)
- **Docker-контейнер**: многоязычный образ на базе `node:20-bookworm-slim` с Python 3.11 и всеми зависимостями.
- **Параллельный процесс (`entrypoint.sh`)**: одновременная работа HTTP API Fastify (порт 10000) и фонового Telegram-бота Aiogram 3.
- **Telegram Bot API**: постоянная привязка кнопки меню чата (`setChatMenuButton`) на публичный адрес `https://project-sa-terminal.onrender.com`.
- **CI/CD Deploy Hook**: моментальный деплой по вебхуку без ручных действий.
- **Полная автономность**: терминал и бот функционируют круглосуточно независимо от состояния локального компьютера оператора.

---

## 4. ЧТО МЫ ПЛАНИРУЕМ / ДУМАЕМ ВВЕСТИ (ROADMAP)

### 🧩 4.1. Развитие Кибер-Симбионта и Лаборатории
- **Внешние кибер-модификации**:
  - Модули за импульсы `⚡` (титановые накладки, оптические световоды, фильтры помех).
- **Связь Симбионта с практиками**:
  - Автоматическое начисление тонуса и импульсов коту при завершении дыхания 4-4-4 или приеме талой воды.
- **Мини-игры калибровки**:
  - «Резонанс сонара» (попадание в ритмическую синусоиду).

### 📚 4.2. Расширение Гримуара
- Наполнение дополнительных прикладных протоколов в 12 существующих категорий.
- Добавление системы полнотекстового поиска по тегам и синапсам Zettelkasten.

### 🛠️ 4.3. Новые инструменты для Оператора
1. **Генератор акустической изоляции (Audio Noise)**:
   - Процедурный синтез белого, розового и коричневого шума через Web Audio API.
2. **Таймер цифровой депривации**:
   - Фиксатор периодов глубокого фокуса без выхода из вкладки (30 / 60 / 120 мин).
3. **Дневник сна и бодрости**:
   - Связка с калькулятором фаз сна для утренней фиксации самочувствия.

### ⚙️ 4.4. Системные улучшения
- **PWA Service Worker**: кэширование стилей и скриптов для гарантированной работы терминала при полном отсутствии интернета.
- **Лимиты на отправку рапортов (`daily_report_caps`)**: максимум 3 послания в сутки для защиты контура от шума.
- **Синхронизация прогресса**: сохранение заметок и тонуса кота через Telegram CloudStorage.
