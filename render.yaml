services:
  - type: web
    name: project-sa-terminal
    env: docker
    plan: free
    region: frankfurt
    healthCheckPath: /api/health
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_TELEGRAM_IDS
        value: "228844325"
